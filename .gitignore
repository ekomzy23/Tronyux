# Secrets — never commit
.env
.env.local

# OS / editor junk
.DS_Store
Thumbs.db
.vscode/

# Vercel
.vercel

# Dependencies
node_modules/

# Obfuscation backups (build.js creates these — never deploy originals over them)
js/*.orig.js

.vercel
